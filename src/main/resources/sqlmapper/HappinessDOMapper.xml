<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.winter.mapper.HappinessDOMapper">
    <!-- 自动生成,请修改 HAPPINESS.xml -->
    <resultMap id="BaseResultMap"  type="com.winter.model.HappinessDO">
        <id column="ID" property="id" jdbcType="INTEGER" javaType="Integer"/>
        <result column="CITY" property="city" jdbcType="VARCHAR" javaType="String"/>
        <result column="NUM" property="num" jdbcType="INTEGER" javaType="Integer"/>
    </resultMap>


    <sql id="Base_SF_Column_List">
        sf.ID,sf.CITY,sf.NUM
    </sql>
    <sql id="Base_Column_List">
        ID,CITY,NUM
    </sql>


    <!--插入表:happiness-->
    <insert id="insert"  >
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT
            LAST_INSERT_ID()
        </selectKey>
        <![CDATA[
        INSERT INTO happiness(
            ID
            ,CITY
            ,NUM
        )VALUES(
             null
            , #{city,jdbcType=VARCHAR}
            , #{num,jdbcType=INTEGER}
        )
        ]]>
    </insert>

    <!--批量插入表:happiness-->
    <insert id="insertBatch"  >
        INSERT INTO happiness(
            ID
            ,CITY
            ,NUM
        )VALUES
        <foreach collection="list" item="item" separator=",">
            (
                 null
                , #{item.city,jdbcType=VARCHAR}
                , #{item.num,jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--根据主键删除数据:happiness-->
    <delete id="deleteById"  >
        <![CDATA[
        DELETE /*MS-HAPPINESS-DELETEBYID*/ FROM happiness
        WHERE
            ID = #{id,jdbcType=INTEGER}
        ]]>
    </delete>

    <!--根据主键获取数据:happiness-->
    <select id="getById" resultMap="BaseResultMap" >
        SELECT /*MS-HAPPINESS-GETBYID*/  <include refid="Base_Column_List" />
        FROM happiness
        WHERE
        <![CDATA[
            ID = #{id,jdbcType=INTEGER}
        ]]>
    </select>
</mapper>
